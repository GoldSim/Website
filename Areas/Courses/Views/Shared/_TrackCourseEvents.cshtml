@model Collection<CourseTrackingEventViewModel>

<script>
  @foreach (var trackingEvent in Model) {
    <text>
      gtag('event', '@trackingEvent.EventName', {
        'course_name': '@trackingEvent.CourseName'
        @if(trackingEvent is UnitTrackingEventViewModel model) {
          <text>
            ,'unit_number': '@(model.UnitNumber)'
          </text>
        }
      });
    </text>
  }
</script>

<!--
  Content Type: Collection<CourseTrackingEventViewModel>
  View Location: ~/Areas/Courses/Views/Shared/_TrackCourseEvents.cshtml
-->